/**
 AlgoExpert
*/


import { PrintMatrix, PrintObject } from '../utils/Utilities';
import SearchA2DMatrix2 from './SearchA2DMatrix2';

const boggleBoard = (original:number[], m:number, n:number): number[][] => {
  return [];
}

export default () => {
    const board = [1,2,3,4]
    console.log(boggleBoard(board, 2, 2));
    // PrintMatrix(board);
};
